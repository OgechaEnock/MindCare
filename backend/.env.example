# Legacy Node backend environment template. The active application backend is
# currently backend1/ (Flask); keep this only if maintaining the Node service.
PORT=4000
DB_USER=postgres
DB_PASSWORD=replace_me
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mental_health_db
ENCRYPTION_KEY=replace_with_a_32_character_minimum_secret
JWT_SECRET=replace_with_a_long_random_secret
JWT_EXPIRES_IN=15m
MODERATION_API_URL=http://localhost:8000/generate/
MODERATION_TIMEOUT_MS=30000
FRONTEND_URL=http://localhost:3000
NODE_ENV=development
